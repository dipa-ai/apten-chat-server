version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations/"
    queries: "internal/db/queries/"
    gen:
      go:
        package: "dbq"
        out: "internal/db/dbq"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
        emit_empty_slices: true
